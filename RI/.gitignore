# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Outputs (regenerated by run_pipeline.py)
results/
figures/

# Jupyter checkpoints
.ipynb_checkpoints/

# OS
.DS_Store

# ---------------------------------------------------------------------------
# Raw ERA5 downloads — external (obtained via Copernicus CDS API in Colab).
# These MUST never be committed: they are large and only reproducible with
# CDS credentials. Place downloads under RI/ERA5_expanded/ (default input for
# build_era5_ri_dataset.py / build_expanded_era5_ri_dataset.py).
# ---------------------------------------------------------------------------
ERA5_expanded/
era5_raw/
downloaded_era5/
*.nc
*.nc4
*.cdsapi
.cdsapirc
cdsapirc

# Temporary Colab / pack artifacts
satellite_cnn_colab_upload.zip

# Downloaded raw satellite granules (recovered via recover_satellite.py /
# pack_satellite_for_colab.py; not source data)
Cnnfiles/

# Local backup copies
*.bak
